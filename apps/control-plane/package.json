{
  "name": "@paperclipai/control-plane",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.10",
  "scripts": {
    "dev": "wrangler dev",
    "typecheck": "tsc --noEmit",
    "test:run": "vitest run --config vitest.config.ts",
    "build": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@paperclipai/shared": "workspace:*",
    "better-auth": "1.4.18"
  },
  "devDependencies": {
    "typescript": "^5.8.2",
    "vitest": "^3.0.8",
    "wrangler": "^4.56.0"
  }
}
